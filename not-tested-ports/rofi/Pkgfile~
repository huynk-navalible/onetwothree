name=rofi
version=2.0.0
release=2.0.0
source=(https://github.com/davatorium/$name/archive/refs/tags/$version.tar.gz)

build() {
	cd $name-$version
	meson setup build
	ninja -C build
	DESTDIR=$PKG ninja -C build install	
}
